# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets — never commit
.env
.env.*
!.env.example

# Source of a different app (beraza-dashboard-api), kept locally as reference only
backend_code/

# Generated by `npm run mail:preview`
preview-*.html

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local tooling config (machine-specific permission grants)
.claude/

# Power Automate flow exports. Reference material for porting the two mails to
# Microsoft Graph; the port is done. They carry the internal SharePoint site
# path and team email addresses, and this repository is public.
EnviarUsuarioYContrase*/
NotificarORdendeCOmpra_*/

# Working documents (client feedback, notes) — not source
*.docx.pdf
